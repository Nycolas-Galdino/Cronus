class Notifier:
    def send_notification(self, message: str, recipient: str):
        # Exemplo simplificado de envio de notificação
        print(f"Enviando '{message}' para {recipient}")
        return True
